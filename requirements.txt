akshare>=1.16
efinance>=0.5         # 个股资金流向(主力净流入),绕过被封的东财 akshare 接口
pandas>=2.0
numpy>=1.24
PyYAML>=6.0
openai>=1.0          # DeepSeek 走 OpenAI 兼容接口 + function calling(agent 层)
python-dotenv>=1.0   # 从 .env 懒加载 DEEPSEEK_API_KEY,密钥不入库
streamlit>=1.32      # Web 仪表盘 run_dashboard.py
