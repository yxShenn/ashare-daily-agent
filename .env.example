# 复制本文件为 .env 并填入真实 key(.env 已被 .gitignore 忽略,不会入库)
#   cp .env.example .env

# DeepSeek API Key(必填,agent 层用)。获取:https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=

# 可选:覆盖默认接口与模型(留空则用 config.yaml 的 agent 配置)
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
# DEEPSEEK_MODEL=deepseek-chat
